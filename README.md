# 🔐 Password Generator

A simple Python program that generates strong and secure passwords using random combinations of uppercase, lowercase letters, digits, and special symbols (`@`, `#`, `$`).

---

## 🚀 Features

* Generates passwords of customizable length
* Includes uppercase, lowercase, digits, and special characters
* Avoids easily guessable patterns
* User-friendly CLI interface

---

## 🧠 How It Works

The program randomly selects characters from:

* Uppercase letters (`A–Z`)
* Lowercase letters (`a–z`)
* Digits (`0–9`)
* Special characters (`@`, `#`, `$`)

It ensures a balanced mix of all character types to strengthen password security.

---

## ▶️ How to Run

1. Make sure Python 3 is installed.
2. Save the file as `PasswordGenerator.py`.
3. Run the program:

   ```bash
   python PasswordGenerator.py
   ```
4. Enter your preferred password length when prompted.

---

## 💡 Example Output

```
Enter the desired password length: 12
Your secure password is: Qr7@Fb9#TcX2
```

---

## 🧰 Technologies Used

* Python 3
* `random` library
